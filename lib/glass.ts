/**
 * Superficies glass sobre el fondo crema PNPJ (receta de Ruta). En este
 * proyecto vive también como `.auth-card-surface`, que añade saturación y el
 * fallback sin `backdrop-filter`.
 */
export const glassCard = "auth-card-surface";
